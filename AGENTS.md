# Agent Rules

1) Before making changes, read PROJECT_STATE.md.
2) If instructions conflict: PROJECT_STATE.md > repo files > chat.
3) Never invent file paths. If unsure, search the repo.
4) When a change spans >2 files:
   - propose plan
   - list files you will touch
   - then execute
5) **Always** work in containers
5) When needed: update the project documentation
6) After major work, update PROJECT_STATE.md and TODO.md
7) After work always run the tests
8) Show me a commit message which reflects the work done
